- **数据库初始化**：需执行`sql/genshin_all.sql`或`sql/rytest.sql`初始化原神业务表结构。
- **构建命令**：使用Maven进行多模块构建，根目录执行`mvn clean package`，或通过`bin/package.bat`脚本打包。
- **启动方式**：运行`ruoyi-admin`模块下的`RuoYiApplication.java`，或使用`java -jar ruoyi-admin.jar`启动。